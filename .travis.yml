language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install codecov
# command to run tests
script:
- pytest -s --cov=Lyrics_Classifier test_main.py

after_success:
    - codecov
